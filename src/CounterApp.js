import React, {useState} from 'react';

const CounterApp = () => {
  const [counter, setCounter] = useState(0);
  return (
    <div>
    <h1>CounterApp</h1>  
    </div>
  )
}

export default CounterApp
